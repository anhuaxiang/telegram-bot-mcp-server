
{
  "name": "mcp-telegram-bot-server",
  "version": "1.0.10",
  "main": "index.js",
  "type": "module",
  "bin": {
    "mcp-telegram-bot-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "jest --config=jest.config.cjs --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "A Model Context Protocol (MCP) server for Telegram Bot API integration",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "telegraf": "^4.16.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "shx": "^0.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
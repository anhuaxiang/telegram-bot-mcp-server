{
  "name": "mcp-telegram-bot-server",
  "version": "1.0.5",
  "description": "A Model Context Protocol (MCP) server for Telegram Bot API integration",
  "main": ".build/index.js",
  "bin": ".build/cli.js",
  "scripts": {
    "build": "npx tsc && shx chmod +x .build/*.js",
    "start": "node .build/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "telegraf": "^4.16.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "shx": "^0.3.4",
    "typescript": "^5.8.3"
  },
  "license": "MIT"
}
